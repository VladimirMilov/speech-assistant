import React from 'react';

class Result extends React.Component{
    
    render(props) {
        // const {next, previous, id} = this.props;
        return (
            <div>
                <h1>Result component</h1>
            </div>
        )
    }
}

export default Result;
